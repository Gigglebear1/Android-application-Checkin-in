<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/clock2"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.bryamie.clockinin.EmployeeLogin" >

    <Button
        android:id="@+id/CheckinButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp"
        android:onClick="CheckinClick"
        android:text="Check In" />

    <Button
        android:id="@+id/view_timeCards"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/CheckinButton"
        android:layout_centerHorizontal="true"
        android:onClick="ClockOutClick"
        android:text="Check out" />

    <Button
        android:id="@+id/TimeCardButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/view_timeCards"
        android:layout_centerHorizontal="true"
        android:onClick="TimeCardClick"
        android:text="Time Card" />

    <Button
        android:id="@+id/editAddEmployee"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/TimeCardButton"
        android:layout_centerHorizontal="true"
        android:onClick="employeeLogoutClick"
        android:text="Logout" />

    <TextView
        android:id="@+id/status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="43dp"
        android:text="TextView" />

</RelativeLayout>
